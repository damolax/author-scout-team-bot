services:
  - type: web
    name: author-scout-team-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bot_main_v3:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
